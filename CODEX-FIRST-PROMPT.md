# First Prompt for Codex

Read `AGENTS.md` and every document inside `docs/` in the order defined in `docs/README.md`.

Then inspect the current repository structure.

Do not write code yet.

Report only:

1. Your understanding of the project goal.
2. The MVP scope.
3. The required architecture.
4. The development phase order.
5. Any conflicting or unclear information.
6. The first task that should be started.

Do not create files.
Do not install dependencies.
Do not modify code.
